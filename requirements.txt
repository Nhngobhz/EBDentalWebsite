# Kept UTF-8: PowerShell's `pip freeze > requirements.txt` writes UTF-16LE with a
# BOM, which pip 24+ refuses to read. Redirect through `Out-File -Encoding utf8`
# if you ever regenerate this file.
Flask
Jinja2
MarkupSafe
Werkzeug
blinker
click
colorama
itsdangerous
python-dotenv
requests
# Production WSGI server - `python app.py` runs Werkzeug's development server,
# which is single-threaded and explicitly not for real traffic.
waitress
# Monitoring: request counters + latency histogram exposed at /metrics for the
# Prometheus service on the server to scrape (metrics.py).
prometheus-client
prometheus-flask-exporter
